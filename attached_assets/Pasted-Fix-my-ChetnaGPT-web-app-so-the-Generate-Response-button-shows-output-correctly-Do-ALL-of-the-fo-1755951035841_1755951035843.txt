Fix my ChetnaGPT web app so the “Generate Response” button shows output correctly.

Do ALL of the following exactly:

1) FRONTEND (index.html)
   - Ensure these elements (IDs MUST match):
     <select id="mode">
       <option value="proposal">Proposal Help Agent</option>
       <option value="business">Business Plan Agent</option>
       <option value="support">Tech Support Agent</option>
     </select>
     <textarea id="input" rows="8" placeholder="Type here..."></textarea>
     <button id="generateBtn">Generate Response</button>
     <div id="loading" class="hidden">⏳ Working...</div>
     <div id="outputArea" class="hidden">
       <pre id="output" class="whitespace-pre-wrap"></pre>
     </div>
   - Add minimal CSS (inline <style> ok):
       .hidden { display:none; }
       .fade-in { animation: fade .25s ease-in; }
       @keyframes fade { from{opacity:0} to{opacity:1} }

2) FRONTEND (app.js)
   - Bind #generateBtn click to POST /chat with body:
       { "mode": document.getElementById('mode').value,
         "input": document.getElementById('input').value.trim() }
   - While waiting: show #loading, hide #outputArea, disable button with text “Generating...”.
   - On success:
       - Hide #loading, show #outputArea.
       - Render response.reply into #output preserving line breaks:
           const safe = (data.reply || "")
                        .replace(/&/g,"&amp;")
                        .replace(/</g,"&lt;")
                        .replace(/\n/g,"<br>");
           output.innerHTML = safe;
       - Add class 'fade-in' and scrollIntoView({behavior:'smooth'}).
   - On error: hide loading, alert the error, re-enable button.
   - Keep the sample input switcher you already made.
   - Add console.log before fetch: console.log("POST /chat", {mode, input: inputText.slice(0,120)+"..."});

3) BACKEND (backend/main.py)
   - Verify FastAPI on port 5000 with:
     a) GET /health -> {"ok": true}
     b) POST /chat -> accepts JSON {"mode": "proposal|business|support", "input": "text"} and returns {"reply":"..."}.
     c) Add CORS allow all (CORSMiddleware).
   - For now, implement simple formatted replies:
       - proposal -> Summary (first 2–3 lines) + sections:
         Introduction, Problem Statement, Proposed Solution, Timeline & Milestones, Pricing & Terms, closing line.
       - business -> Summary + bullets:
         Vision & Mission, Target Market, Go-to-Market, Revenue Model, Operations & Team, 3-month Roadmap (≤500 words).
       - support -> Summary + “Likely causes (1–2)” + “Fix steps (2–5 with commands)” + optional clarifying question.

4) SERVE FRONTEND
   - Ensure FastAPI serves /frontend as static (html=True) so preview root loads index.html.

5) RUN COMMAND
   - requirements.txt includes: fastapi, uvicorn[standard]
   - Run command: python backend/main.py

6) QUICK TESTS (confirm and show in reply)
   - Open /health (expect {"ok": true}).
   - In UI:
     • proposal + “Restaurant: online ordering + CRM + delivery, $15k, 3 months” -> structured proposal appears.
     • business + “AI fitness app, millennials, subscription ₹299/month” -> 1-page plan appears.
     • support + “npm ERR! module not found: react-dom” -> causes + steps appear.

Explain changes you made and confirm the preview URL works.